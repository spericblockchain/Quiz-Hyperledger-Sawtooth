export interface StudentModel {
  name: string
  mobile: string
  subject: string
  batch: string
  publicKey: string
  address: string
}
export interface StudentDataModel {
  name: string
  mobile: string
  subject: string
  batch: string
  playedQuiz: string[]
}
